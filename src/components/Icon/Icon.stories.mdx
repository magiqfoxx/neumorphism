import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import Icon, { names, sizes, colors } from "./Icon";
const listOfVariants = ["light"];

<Meta
  title="MDX/Icon"
  component={Icon}
  decorators={[withKnobs]}
  parameters={{
    backgrounds: [
      { name: "light", value: colors.light, default: true },
      { name: "dark", value: colors.dark },
    ],
  }}
/>

# Icon

With `MDX` we can define a story for `Icon` right in the middle of our
markdown documentation.

<Preview>
  <Story name="simple">
    <Icon
      name={select("name", names, "light")}
      size={select("size", sizes, "light")}
      color={select("color", colors, "light")}
    ></Icon>
  </Story>
</Preview>
