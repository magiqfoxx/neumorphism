import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import Toggle from './Toggle';
import colors from "../../tokens/colors";
const listOfVariants=['light'];

<Meta title="MDX/Toggle" component={Toggle} decorators={[ withKnobs]} parameters={{backgrounds: [{ name: 'light', value: colors.light, default: true },{ name: 'dark', value: colors.dark }]}} />

# Toggle

With `MDX` we can define a story for `Toggle` right in the middle of our
markdown documentation.

<Preview>
  <Story name="text only">
      <Toggle variant={select("variant", listOfVariants, 'light')} checked={boolean("checked", true)}></Toggle>
  </Story>
</Preview>